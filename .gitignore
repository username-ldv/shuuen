# Each project keeps its own .gitignore (app/, backend/, web/).

# OS and editor files at the repository root
.DS_Store
Thumbs.db
/.idea/

# Production secrets for the Docker stack (see .env.example)
/.env

# Claude Code personal settings
.claude/settings.local.json
